sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t JavierGold/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true JavierGold/docker-react npm run test